{
  "description": "A line that parses as JSON but is an array, not an object, is refused as not-object before wrong-message is ever reached — §5.1 step 2 precedes step 3, and no other case in this corpus exercises that ordering on the parse side, since every other non-object parse input this corpus carries is invalid JSON to begin with.",
  "kind": "parse",
  "line": "[]",
  "expect": {
    "ok": false,
    "reason": "not-object",
    "path": ""
  }
}
