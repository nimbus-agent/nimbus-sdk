{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/nimbus-agent/nimbus-sdk/main/docs/spec/conformance/v1/canonical-json/index.schema.json",
  "title": "Canonical-JSON corpus index",
  "description": "Machine-readable manifest of the canonical-json cases, so a runner in any language consumes the corpus without parsing prose.",
  "type": "object",
  "required": ["spec", "cases"],
  "additionalProperties": false,
  "properties": {
    "spec": {
      "const": "../../../signing/v1/canonical-json.md",
      "description": "The normative document this corpus is the executable form of."
    },
    "cases": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["file", "section", "reason"],
        "additionalProperties": false,
        "properties": {
          "file": {
            "type": "string",
            "pattern": "^cases/[A-Za-z0-9._-]+\\.json$",
            "description": "Case path relative to this directory. No path separators in the filename, so an entry cannot reach outside the corpus."
          },
          "section": {
            "type": "string",
            "pattern": "^§[0-9]+(\\.[0-9]+)*$",
            "description": "The specification section this case pins. Subsection-capable like the diagnostics corpus, because §5's two malformed conditions are separately citable."
          },
          "reason": { "type": "string", "pattern": "\\S" }
        }
      }
    }
  }
}
